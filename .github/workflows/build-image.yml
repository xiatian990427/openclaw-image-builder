name: Build and Export OpenClaw Image

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Pull OpenClaw image (mirror)
        run: docker pull dockerproxy.com/pspdfkit/openclaw:latest

      - name: Save image to tar file
        run: docker save dockerproxy.com/pspdfkit/openclaw:latest -o openclaw.tar

      - name: Compress the image
        run: gzip openclaw.tar

      - name: Upload image as artifact
        uses: actions/upload-artifact@v4
        with:
          name: openclaw-docker-image
          path: openclaw.tar.gz
